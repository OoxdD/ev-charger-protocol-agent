---
description: 用户要求提交 Git 时，同步更新改动与工作汇总.md
alwaysApply: true
---

# Git 提交时更新工作汇总

每当用户明确要求**提交代码到 Git**（或 push 一并提交）时，在执行 commit 之前必须：

1. 更新项目内文档：`remark/改动与工作汇总.md`
2. 把**本次即将提交**的改动记进去（不要只写 commit hash，要把业务含义写清楚）
3. 将该汇总文件一并纳入本次 commit（除非用户明确说不要提交文档）

## 写入格式要求

在 `remark/改动与工作汇总.md` 中：

- 在「已提交到 Git 的版本」相关章节**顶部新增一条**（时间倒序：最新在上）
- 写清：日期、拟用/已用的 commit 说明、需求要点、落地效果、主要涉及文件
- 若有「尚未提交」章节，提交后把对应条目迁入「已提交」，并清理过时的未提交说明
- 保持与现有文档风格一致：中文、表格优先、面向协作可读

## 注意

- 仅在用户明确要求 commit 时更新并提交该文件；平时改代码不要主动为此单独开 commit
- 客户版 `remark/工作记录-客户版.md` 默认不自动改；仅当用户明确要求同步客户版时再更新
